package dev.nalak1206.ashtracer.soul;

/**
 * Semantic intent states. Visuals must communicate shape, rhythm and motion;
 * a single color must never become a direct "dodge now" indicator.
 */
public enum SoulIntent {
	DORMANT,
	HUNTING,
	GATHERING,
	FEINTING,
	BREAKING,
	HOLLOW
}
